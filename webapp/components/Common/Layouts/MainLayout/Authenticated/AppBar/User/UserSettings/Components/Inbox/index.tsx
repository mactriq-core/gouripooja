// React
import React from "react";

// MUI
import { Box, Typography } from "@mui/material";

const Inbox = () => {
  return (
    <Box className="flex-1 flex flex-col">
      <Typography variant="button" sx={{ m: "auto", textAlign: "center" }}>
        Under Development
      </Typography>
    </Box>
  );
};

export default Inbox;
